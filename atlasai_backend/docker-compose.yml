version: "3.9"

services:
  api:
    build: .
    container_name: atlasai_api

    ports:
      - "8000:8000"

    volumes:
      - ./app:/code/app
      - ./chroma_db:/code/chroma_db

    env_file:
      - .env

    restart: unless-stopped